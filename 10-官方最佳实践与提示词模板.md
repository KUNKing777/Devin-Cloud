# 第十章：官方最佳实践与提示词模板

> 本章对照 Devin 官方文档中的 Essential Guidelines 补充：什么时候适合用 Devin、怎样写任务、怎样验收结果。
> 目标是让新手不再只会说“帮我改一下”，而是能写出 Devin 看得懂、做得稳、方便你验收的任务。

---

## 10.1 先判断：这个任务适不适合交给 Devin？

一句话：**只要你能说清楚“要做什么、给 Devin 看什么、怎么判断完成”，就适合交给 Devin。**

官方建议你在开始前问自己 3 个问题：

| 问题 | 新手解释 | 如果答案是“否”怎么办 |
|------|----------|----------------------|
| 我能说清成功标准吗？ | 例如“登录按钮能跳转”“测试全部通过”“PR 里有截图” | 先用 Ask Devin 帮你梳理范围 |
| Devin 有足够上下文吗？ | 相关文件、旧实现、设计图、接口文档、报错截图 | 在任务里贴链接、文件名、截图或错误日志 |
| 任务会不会太大？ | 你自己 3 小时内能做完，Devin 通常也更容易做好 | 拆成多个小 Session，必要时并行跑 |

### 适合 Devin 的任务

- 修 bug：有复现步骤、报错日志、期望行为。
- 加功能：有清楚需求、入口页面、接口或设计参考。
- 写测试：说明要覆盖哪个模块、跑什么测试命令。
- 重构：说明保持什么接口不变、参考哪个现有模式。
- 文档：说明读者是谁、要补哪些章节、语言风格。
- 代码迁移：说明从什么版本迁移到什么版本、兼容要求。
- 排查线上问题：提供监控、日志、Sentry、Datadog、数据库或 MCP 入口。

### 不太适合直接交给 Devin 的任务

| 模糊说法 | 问题 | 更好的做法 |
|----------|------|------------|
| “把系统优化一下” | 不知道优化哪里 | “优化订单列表接口，目标是 p95 从 2s 降到 500ms 以内” |
| “把页面做漂亮点” | 审美没有标准 | 提供 Figma、截图、颜色、间距、参考页面 |
| “全面重构项目” | 范围太大 | 先让 Ask Devin 分析，再拆成“认证模块 / 支付模块 / 页面组件” |
| “修所有问题” | 没有完成标准 | 列出具体问题，或先让 Devin 分类出优先级 |

---

## 10.2 Devin 工作前检查清单

发任务前，按下面 5 项检查。能填多少填多少，不用追求完美。

```markdown
## 任务目标
我希望 Devin 完成什么？

## 背景
为什么要做？现在的问题是什么？

## 相关位置
仓库、分支、文件、页面、接口、报错日志、设计图链接。

## 约束
不能改什么？必须保持什么兼容？是否必须走 PR？

## 验收标准
跑哪些命令？看哪个页面？什么结果算完成？
```

### 新手记忆法：3 句话公式

1. **背景**：现在哪里不对。
2. **任务**：你要 Devin 改什么。
3. **验收**：怎么证明改好了。

例子：

```text
背景：用户在设置页点击“保存头像”后，页面显示成功但刷新后头像丢失。
任务：请排查并修复头像保存逻辑，优先复用现有 user profile API。
验收：添加或更新测试；运行 lint 和相关测试；打开 PR，并在 PR 说明里写清楚原因和修复方式。
```

---

## 10.3 Ask Devin：先问清楚，再让 Agent 动手

很多新手一上来就开 Agent 模式让 Devin 改代码。更稳的流程是：

1. **先用 Ask Devin 探索代码库**
   - 问：“这个仓库的登录流程在哪些文件里？”
   - 问：“如果我要加微信登录，应该改哪些地方？”
   - 问：“请帮我生成一个适合交给 Agent 的任务描述。”
2. **确认计划**
   - 看 Devin 找到的文件是否合理。
   - 看它建议的改动范围是否太大。
3. **再从 Ask Devin 启动 Agent Session**
   - 让 Agent 带着完整上下文去实现。

### 适合 Ask Devin 的问题

```text
帮我理解这个仓库的认证模块结构，不要改代码。
```

```text
我想新增一个导出 CSV 功能，请先分析需要改哪些文件，并给我一个实现计划。
```

```text
这个报错可能来自哪里？请结合代码搜索给出排查路线，不要直接修改。
```

---

## 10.4 好提示词 vs 坏提示词

### 坏提示词：太短、太空

```text
修一下登录问题
```

问题：

- 没说哪个登录入口。
- 没说现象和复现步骤。
- 没说期望行为。
- 没说怎么验证。

### 好提示词：有范围、有上下文、有验收

```text
仓库：my-web-app

问题：
在 /login 页面输入正确账号密码后，点击 Login 会一直转圈，
浏览器控制台报错：POST /api/login 500。

请你：
1. 复现这个问题并定位原因。
2. 修复登录接口或前端调用中的问题。
3. 不要改动注册流程。
4. 添加或更新相关测试。
5. 运行 lint 和测试。
6. 创建 PR，并在 PR 描述中写清楚复现、原因、修复和验证结果。

验收标准：
- 正确账号密码可以进入 dashboard。
- 错误账号密码显示错误提示，不会白屏。
- CI 通过。
```

---

## 10.5 常用任务模板

下面这些模板可以直接复制，然后把括号内容换成你的实际信息。

### 模板 A：修复具体 bug

```text
请修复这个 bug：

现象：
（用户看到什么错误？）

复现步骤：
1. 打开（页面/功能）
2. 点击（按钮）
3. 输入（数据）
4. 看到（错误）

期望行为：
（正确情况下应该发生什么？）

相关信息：
- 仓库：
- 分支：
- 相关文件：
- 报错日志/截图：

要求：
- 先复现或解释为什么无法复现。
- 找到根因，不要只做表面绕过。
- 添加或更新测试。
- 跑 lint/test。
- 创建 PR。
```

### 模板 B：实现新功能

```text
请实现这个功能：

功能目标：
（一句话说明用户能做什么）

用户流程：
1. 用户进入（页面）
2. 用户点击（入口）
3. 用户填写/选择（内容）
4. 系统展示（结果）

设计/接口参考：
- 设计图：
- API 文档：
- 参考文件：

边界情况：
- 空数据时：
- 权限不足时：
- 接口失败时：

验收标准：
- UI 与现有风格一致。
- 关键路径可以在浏览器中跑通。
- 相关测试通过。
- 创建 PR，说明实现思路和验证方式。
```

### 模板 C：重构代码

```text
请重构（模块/文件）：

目标：
（例如：降低重复代码、拆分复杂组件、迁移到现有新模式）

必须保持不变：
- 对外 API：
- 用户行为：
- 数据格式：

参考模式：
- 请参考（文件名/目录）里的写法。

要求：
- 不要顺手改无关功能。
- 每一步保持可运行。
- 添加或更新测试，证明行为没有变。
- PR 描述里写清楚重构前后结构变化。
```

### 模板 D：增加测试覆盖

```text
请为（模块/功能）补充测试：

需要覆盖：
- 正常路径：
- 异常路径：
- 边界情况：

参考现有测试：
- （测试文件路径）

要求：
- 使用项目现有测试框架和风格。
- 不要为了让测试通过而改业务逻辑，除非发现真实 bug。
- 运行相关测试，并在 PR 里写明测试命令。
```

### 模板 E：更新文档

```text
请更新（文档名称）：

读者：
（新手 / 开发者 / 运维 / 团队管理员）

目标：
读者看完后应该能完成（某件事）。

需要补充：
- 背景解释
- 操作步骤
- 常见错误
- 示例
- 验收方式

要求：
- 用通俗中文。
- 少用黑话，必须出现的术语先解释。
- 步骤要能照着点。
- 更新目录和交叉链接。
```

### 模板 F：排查线上问题

```text
请排查这个线上问题：

影响：
（影响哪些用户/页面/接口？严重程度？）

现象：
（报错、截图、日志、告警）

可用工具：
- Sentry：
- Datadog：
- 数据库：
- 日志：
- 相关 MCP：

要求：
- 先汇总时间线和可能原因。
- 不要直接改生产配置。
- 如果需要访问密钥或权限，先说明最小权限需求。
- 提出修复方案，再实现低风险修复。
- 创建 PR，并写清楚验证方式和回滚方案。
```

---

## 10.6 用 Slash Commands 提速

在 Devin 输入框里输入 `/`，可以看到内置命令。它们相当于“官方提示词快捷键”。

| 命令 | 什么时候用 | 新手说法 |
|------|------------|----------|
| `/plan` | 先分析、先规划 | “别急着改，先告诉我怎么做” |
| `/implement` | 需求已经清楚，直接实现 | “按这个方案开工” |
| `/test` | 写测试、跑测试、查覆盖率 | “帮我验证它真的能跑” |
| `/review` | 审查代码或 PR | “帮我挑 bug 和风险” |
| `/think-hard` | 复杂问题需要更仔细推理 | “这个问题很绕，多想一步” |

建议：

- 大任务先 `/plan`，确认后再 `/implement`。
- 改完前后都可以 `/test`。
- 合并前用 `/review` 多一道保险。

---

## 10.7 让 Devin 自己验证结果

不要只让 Devin “写完代码”。更好的要求是让它**闭环**：

1. 读代码和需求。
2. 修改代码。
3. 跑 lint、测试、类型检查或构建。
4. 如果是前端，启动应用并用浏览器点一遍。
5. 创建 PR。
6. 根据 CI、Devin Review 或你的 review 继续修。
7. 必要时录制测试视频发给你。

### 你可以这样说

```text
完成后请自己验证：
- 运行 npm run lint
- 运行 npm test
- 启动本地应用
- 用浏览器验证新增流程
- 如果能测试通过，请在 PR 描述中写明验证命令和结果
```

如果涉及 UI，还可以加：

```text
PR 创建后，请测试核心用户路径并录制视频给我。
```

---

## 10.8 任务结束后怎么复盘？

官方建议用 Session Insights 和实际结果复盘。新手可以看 4 件事：

| 看什么 | 说明 | 下次怎么改 |
|--------|------|------------|
| Devin 卡在哪里 | 环境、依赖、权限、需求不清？ | 补 Knowledge、Secrets、环境配置 |
| 是否多次问你问题 | 说明上下文不够 | 下次任务里提前提供文件、日志、截图 |
| ACU 是否很高 | 可能任务太大或环境反复安装 | 拆任务、配置 Snapshot |
| PR 是否容易 review | 说明任务范围是否聚焦 | 一次只做一个主题 |

### 复盘后最有价值的 3 个沉淀

1. **Knowledge**：把团队偏好、常见命令、注意事项写进去。
2. **AGENTS.md / Skills**：把仓库级流程、测试步骤、部署步骤沉淀到代码库。
3. **Blueprint**：把依赖安装、环境变量、启动命令放进环境配置，避免每次重来。

---

## 10.9 新手最稳工作流

如果你不知道怎么开始，照这个流程：

```text
1. Ask Devin：先问代码结构和实现计划
2. 你确认范围：删掉不必要的部分
3. Agent Session：让 Devin 实现
4. Devin 自测：lint/test/build/browser
5. Devin 开 PR
6. Devin Review + CI
7. 你看 PR 描述、diff、测试结果
8. 没问题再 Merge
9. 把这次学到的命令沉淀到 Knowledge / Skill / Blueprint
```

一句话总结：

> **先讲清楚，再让它做；做完要验证；验证过再合并；踩过的坑要沉淀。**

---

## 官方参考

- Essential Guidelines：https://docs.devin.ai/zh/essential-guidelines
- When to Use Devin：https://docs.devin.ai/essential-guidelines/when-to-use-devin
- Instructing Devin Effectively：https://docs.devin.ai/essential-guidelines/instructing-devin-effectively
- Good vs. Bad Instructions：https://docs.devin.ai/essential-guidelines/good-vs-bad-instructions
- Prompt Templates Cheat Sheet：https://docs.devin.ai/essential-guidelines/prompt-templates-cheat-sheet
- Slash Commands：https://docs.devin.ai/work-with-devin/slash-commands

# 第十一章：进阶功能与自动化

> 前面章节已经能让你完成“让 Devin 改代码、开 PR、做 review”的基础流程。
> 本章补齐官方文档里容易被新手漏掉的能力：环境配置、Skills、MCP、自动化、录屏测试、Session Insights、Devin CLI。

---

## 11.1 一张图理解 Devin 的能力边界

你可以把 Devin 当作一个在云端工作的工程师，它有：

| 能力 | 新手理解 | 典型用途 |
|------|----------|----------|
| 代码搜索 | 能读你的仓库 | 找文件、理解架构、定位 bug |
| Shell | 能跑命令 | 安装依赖、跑测试、构建项目 |
| IDE | 能看和改代码 | 实时查看 diff、必要时接管 |
| Browser / Desktop | 能打开网页点按钮 | 登录、UI 测试、录屏验证 |
| Git 集成 | 能开 PR、处理 CI | 标准代码协作 |
| Knowledge | 能记住团队规则 | 长期偏好、常见命令 |
| Playbook | 能复用任务模板 | 发布、巡检、周报 |
| Skill | 能复用仓库内操作流程 | “这个项目怎么测试” |
| MCP | 能连接外部工具 | 数据库、Sentry、Figma、Notion、Stripe |
| Automation / Schedule | 能自动触发任务 | 定期巡检、告警处理、依赖升级 |

---

## 11.2 环境配置：让 Devin 每次开机就能干活

如果不配置环境，Devin 每次 session 都要重新摸索：

- 这个项目用 Node、Python、Java 还是 Go？
- 依赖怎么装？
- 测试命令是什么？
- 环境变量在哪里？
- 本地服务怎么启动？

配置好环境后，Devin 启动时会从 **Snapshot（快照）** 开始，里面已经装好工具和依赖。

### 新手优先级

| 优先级 | 要做什么 | 放在哪里 |
|--------|----------|----------|
| 1 | 依赖安装命令 | Blueprint |
| 2 | 测试、lint、build 命令 | Blueprint 或 Skill |
| 3 | 必要环境变量名 | Blueprint + Secrets |
| 4 | 登录状态、站点 Cookie | Secrets & Site Cookies |
| 5 | 项目特殊流程 | AGENTS.md 或 Skill |

### Blueprint 是什么？

Blueprint 是 Devin 环境的“装机脚本”。例如：

```yaml
initialize:
  - run: npm install
  - run: npm run build

maintenance:
  - run: git pull
  - run: npm install
```

通俗解释：

- `initialize`：第一次构建快照时做的事。
- `maintenance`：后续更新快照时做的事。
- Snapshot：这些命令成功后保存出来的“可直接开工的机器”。

### Git-backed Blueprint

高级一点的团队可以把 Blueprint 放到仓库里：

```text
.devin/blueprint.yaml
```

好处：

- 环境配置跟代码一起 review。
- 修改环境也可以走 PR。
- 新成员和 Devin 都用同一套配置。

---

## 11.3 AGENTS.md、Knowledge、Playbook、Skill 的区别

这几个概念很容易混。记住下面这张表：

| 名称 | 放在哪里 | 适合写什么 | 例子 |
|------|----------|------------|------|
| AGENTS.md | 仓库里 | 对这个仓库长期生效的规则 | “不要改 generated 文件” |
| Knowledge | Devin 平台里 | 团队偏好、账号级经验 | “本团队 PR 描述必须包含测试结果” |
| Playbook | Devin 平台里 | 可重复执行的任务模板 | “每周依赖升级流程” |
| Skill | 仓库 `.agents/skills/` | 可执行的项目流程 | “启动前端并录屏测试” |

### 什么时候用 AGENTS.md？

当规则和仓库强相关，并且你希望所有协作者都能在代码里看到时，用 AGENTS.md。

示例：

```markdown
# AGENTS.md

## Setup
- 使用 pnpm，不要使用 npm。
- 安装依赖：pnpm install。

## Test
- 前端改动后运行 pnpm lint 和 pnpm test。
- 修改数据库 schema 后运行 pnpm prisma generate。

## Code Style
- 不要手动修改 generated 文件。
- 新组件放在 src/components。
```

### 什么时候用 Knowledge？

当它是组织经验、长期偏好、跨仓库规则时，用 Knowledge。

适合写：

- “这个团队默认走 PR，不直接 push main。”
- “线上数据库只能只读查询。”
- “遇到某类错误先查 Sentry 再查日志。”

不适合写：

- 一次性的任务要求。
- 很长、很复杂、需要按步骤执行的流程。
- 真实密钥明文。

### 什么时候用 Playbook？

当你经常重复同一种任务时，用 Playbook。

例如：

- “每周升级依赖并开 PR”
- “新项目初始化检查”
- “线上告警排查流程”
- “发布前 checklist”

### 什么时候用 Skill？

当 Devin 需要在仓库里反复执行一套具体步骤时，用 Skill。

常见 Skill：

- 如何启动本地应用。
- 如何登录测试账号。
- 如何跑 e2e 测试。
- 前端改动后如何截图或录屏。

Skill 文件一般放在：

```text
.agents/skills/某个技能名/SKILL.md
```

并带有开头信息：

```markdown
---
name: test-webapp
description: Run the local web app and verify core user flows before PR.
---

## Setup
1. npm install
2. npm run dev

## Verify
1. Open http://localhost:3000
2. Test login
3. Test the changed page
```

---

## 11.4 Secrets 与 Site Cookies：安全地给 Devin 凭证

不要把密码、Token、API Key 直接发在普通聊天里，也不要写进仓库。

Devin 官方提供了几类安全方式：

| 类型 | 适合什么 | 例子 |
|------|----------|------|
| Persisted Global Secrets | 长期可复用的密钥 | GitHub token、云服务 key |
| Repo-Specific Secrets | 只给某个仓库使用 | 某项目的 `DATABASE_URL` |
| Session-Specific Secrets | 只给这次 session 用 | 临时测试账号密码 |
| Site Cookies | 浏览器登录状态 | 后台管理系统登录 |
| One-Time Password / TOTP | 2FA 动态验证码 | 需要 2FA 的服务 |

### 新手安全原则

1. **能给最小权限，就不要给管理员权限。**
2. **能给临时密钥，就不要给长期密钥。**
3. **密钥只放 Secrets，不写进代码和文档。**
4. **告诉 Devin 变量名，不要让它猜。**

示例任务描述：

```text
这个项目需要 DATABASE_URL 和 STRIPE_SECRET_KEY。
请如果缺少密钥就向我请求，不要把密钥写入仓库。
```

---

## 11.5 MCP：把 Devin 连接到外部世界

MCP（Model Context Protocol）可以理解成“给 Devin 安装外部工具接口”。

没有 MCP 时，Devin 主要能看代码和网页。
有 MCP 后，Devin 可以在授权范围内访问更多系统。

### 常见 MCP 用法

| MCP | Devin 能做什么 |
|-----|----------------|
| Sentry | 查错误、看堆栈、定位线上 bug |
| Datadog | 查监控、日志、性能指标 |
| Figma | 读设计稿，按设计实现 UI |
| Notion | 读取产品文档、需求说明 |
| Stripe | 查支付对象、理解账单流程 |
| Supabase / Neon | 查询数据库或 schema |
| Jira / Linear | 读取工单、更新任务状态 |

### 给新手的使用建议

如果任务需要外部信息，在任务里直接说明：

```text
请通过 Sentry MCP 查看最近 24 小时 /checkout 的错误，
结合代码定位根因，并开 PR 修复。
```

```text
请根据 Figma MCP 中的设计稿实现设置页，
并用浏览器截图验证布局。
```

注意：涉及数据库、支付、生产系统时，要限制权限，优先只读。

---

## 11.6 Devin Review、Auto-Review、Auto-Fix

Devin Review 是专门帮助你审 PR 的能力。

它可以：

- 解释复杂 PR。
- 找潜在 bug。
- 标记安全或逻辑风险。
- 对 PR 做 Auto-Review。
- 在开启 Auto-Fix 后，让 Devin 自动修复评论和 CI 问题。

### 新手怎么用

1. Devin 开 PR 后，先看 PR 描述。
2. 再看 Devin Review 的发现。
3. 如果只是小问题，让 Devin 修。
4. 如果是产品方向或架构取舍，你自己决定。
5. 等 CI 和 Review 都没问题，再 merge。

### Auto-Fix 适合什么

适合：

- lint 错误
- 测试失败
- 小 bug
- reviewer 指出的明确问题

不适合完全自动处理：

- “要不要改产品逻辑”
- “要不要换架构”
- “要不要放宽安全策略”

---

## 11.7 让 Devin 录屏测试

Devin 有完整桌面环境，可以：

- 启动本地应用。
- 用浏览器打开页面。
- 点击按钮、填写表单。
- 截图。
- 录制测试视频。

### 什么时候应该要求录屏？

| 场景 | 是否推荐录屏 |
|------|--------------|
| 改 README | 不需要 |
| 改后端纯逻辑 | 通常不需要，跑测试即可 |
| 改登录/支付/表单 | 推荐 |
| 改布局/样式 | 强烈推荐 |
| 修复“点按钮没反应” | 强烈推荐 |

### 任务里可以这样写

```text
PR 创建后，请测试核心用户流程并录屏：
1. 打开登录页
2. 登录测试账号
3. 进入设置页
4. 修改昵称并保存
5. 刷新页面确认昵称仍然存在
```

录屏不是替代测试套件，而是给 reviewer 一个直观证据：**这个核心流程真的跑通过。**

---

## 11.8 Automations 与 Scheduled Sessions

如果你发现自己总是在重复给 Devin 派同样的任务，就可以自动化。

### Scheduled Sessions：按时间执行

适合：

- 每周一检查依赖更新。
- 每天早上生成错误报告。
- 每周跑一次测试覆盖率分析。
- 每月整理技术债清单。

示例：

```text
每周一上午 9 点，检查本仓库依赖是否有安全更新。
如果有，升级、跑测试、开 PR，并在 PR 里写明风险。
```

### Automations：按事件触发

适合：

- Slack 里出现告警时自动 triage。
- GitHub 新 issue 打上某个 label 后自动启动 Devin。
- Linear/Jira 工单分配给 Devin 后自动开 session。
- Webhook 收到外部系统事件后触发。

### 新手建议

先手动跑 2-3 次，确认流程稳定，再做自动化。
不要一开始就把生产高风险操作自动化。

---

## 11.9 Slack、Teams、Jira、Linear 集成

这些集成的意义是：**不用每次打开 Devin 网页，也能从团队日常工具里启动 Devin。**

### Slack / Teams

适合从讨论中直接派活：

```text
@Devin 这个报错看起来和 checkout 有关，请根据线程里的日志排查并开 PR。
```

Devin 会在线程里更新进度。

### Jira / Linear

适合把工单变成 PR：

- 把 ticket 分配给 Devin。
- 或打上 Devin/Playbook 相关 label。
- 或在评论里 @Devin。

新手建议：

- 工单标题写清楚目标。
- 工单描述写复现步骤、验收标准、设计链接。
- 不要只写“优化一下”。

---

## 11.10 Devin CLI 与 Handoff

Devin CLI 适合喜欢在本地终端工作的开发者。

你可以：

- 在本地项目里直接和 Devin 对话。
- 让 Devin 结合你本地上下文分析问题。
- 需要长时间云端执行时，用 `/handoff` 交给 Cloud Devin。

安装命令：

```bash
curl -fsSL https://cli.devin.ai/install.sh | bash
```

典型场景：

1. 你本地正在看代码。
2. 想让 Devin 帮你分析一个报错。
3. 发现任务比较大。
4. 使用 `/handoff` 让云端 Devin 继续完成、开 PR、跑测试。

---

## 11.11 Session Tools：什么时候看 Shell、IDE、Browser？

Devin 工作时，你可以观察它的工具。

| 工具 | 你看它干嘛 | 什么时候介入 |
|------|------------|--------------|
| Progress | 看 Devin 当前计划和进度 | 方向明显跑偏时 |
| Shell | 看安装、测试、构建命令 | 命令反复失败时 |
| IDE | 看代码 diff | 改了不该改的文件时 |
| Browser/Desktop | 看 UI 测试 | 登录、视觉、表单流程需要你确认时 |

如果你接管 IDE 或浏览器改了东西，记得告诉 Devin：

```text
我刚才手动修改了 src/pages/Login.tsx，把按钮文案改成了“登录”。
请基于这个状态继续。
```

---

## 11.12 Session Insights：让下一次更省钱、更顺

Session 结束后，可以看 Session Insights。

它主要帮你回答：

- Devin 花了多少 ACU？
- 是不是任务太大？
- 是不是因为环境没配好浪费时间？
- 哪些 Knowledge 被用到了？
- 有没有可执行的改进建议？

### 看到这些信号要处理

| 现象 | 可能原因 | 改进 |
|------|----------|------|
| ACU 很高但产出少 | 环境反复失败、需求太大 | 配 Blueprint、拆任务 |
| Devin 一直问你问题 | 任务上下文不足 | 提供文件、日志、设计图 |
| 同类问题反复出现 | 知识没沉淀 | 加 Knowledge / Skill |
| 每次都重新装依赖 | Snapshot 没配置好 | 更新 Blueprint |

---

## 11.13 安全、权限与费用常识

### 安全

- 只给 Devin 完成任务所需的最小权限。
- 不要在普通消息、README、代码里写密钥。
- 团队仓库使用 PR + Review + CI。
- 生产数据优先只读。
- 重要仓库开启分支保护。

### 费用

Devin 的使用会消耗额度或 ACU。想省额度：

1. 任务拆小。
2. 环境提前配好。
3. 提供清楚上下文。
4. 使用 Ask Devin 先规划。
5. 让 Session 保持聚焦，不要一个会话塞十件事。

---

## 11.14 官方功能补全清单

如果你想把团队 Devin 使用体系搭完整，按这个顺序做：

```text
第 1 步：连接 GitHub / GitLab / Bitbucket
第 2 步：为核心仓库配置 Blueprint 和 Snapshot
第 3 步：添加必要 Secrets 和 Site Cookies
第 4 步：创建 AGENTS.md，写清仓库规则
第 5 步：为测试、部署、登录流程创建 Skills
第 6 步：沉淀团队 Knowledge
第 7 步：把重复任务做成 Playbook
第 8 步：连接 Slack / Teams / Jira / Linear
第 9 步：按需接入 MCP（Sentry、Datadog、Figma、数据库等）
第 10 步：开启 Devin Review / Auto-Review / Auto-Fix
第 11 步：把稳定重复流程升级为 Scheduled Session 或 Automation
第 12 步：定期查看 Session Insights，持续优化提示词和环境
```

---

## 官方参考

- Environment configuration：https://docs.devin.ai/onboard-devin/environment
- Declarative configuration：https://docs.devin.ai/onboard-devin/environment/blueprints
- AGENTS.md：https://docs.devin.ai/onboard-devin/agents-md
- Knowledge：https://docs.devin.ai/product-guides/knowledge
- Playbooks：https://docs.devin.ai/product-guides/creating-playbooks
- Skills：https://docs.devin.ai/product-guides/skills
- Secrets & Site Cookies：https://docs.devin.ai/product-guides/secrets
- MCP Marketplace：https://docs.devin.ai/work-with-devin/mcp
- Devin Review：https://docs.devin.ai/work-with-devin/devin-review
- Testing & Video Recordings：https://docs.devin.ai/work-with-devin/testing-and-recordings
- Automations：https://docs.devin.ai/product-guides/automations
- Scheduled Sessions：https://docs.devin.ai/product-guides/scheduled-sessions
- Session Insights：https://docs.devin.ai/product-guides/session-insights
- Devin CLI：https://docs.devin.ai/work-with-devin/devin-cli

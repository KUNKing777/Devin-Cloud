# 第十二章：团队管理与生产工作流

> 本章补齐团队邀请、编辑器设置导入、自动分流、部署边界和机器人评论自动修复。它们不是每个新手第一天都要开，但团队正式使用 Devin 时很重要。

---

## 12.1 本章解决什么问题？

| 你遇到的问题 | 应该看哪一节 |
|--------------|--------------|
| 想邀请同事加入同一个 Devin 组织 | 12.2 |
| 想把本地 VS Code 设置和扩展带进 Devin IDE | 12.3 |
| Slack 的 bug 太多，想自动调查和分流 | 12.4 |
| 想让 Devin 部署应用 | 12.5 |
| 想让 Devin 自动处理 CI、扫描器或 Review Bot 的评论 | 12.6 |

如果你还没配置仓库和环境，请先完成第一、二章。

---

## 12.2 邀请团队成员

### 使用条件

邀请团队功能只适用于 **Teams** 和 **Enterprise** 方案。个人 Free、Pro、Max 账号不能靠共享登录变成团队账号。

### 两种角色

| 角色 | 可以做什么 |
|------|------------|
| Member | 启动 Session，查看和使用组织的 Knowledge、Playbook、环境等 |
| Admin | 拥有 Member 权限，还能管理账单、集成、Secrets 和组织设置 |

角色控制权限；Full seat / Flex seat 控制计费和使用方式。这是两套不同概念。

### 邀请步骤

1. 管理员打开 **Settings → Members**。
2. 点击 **Invite members**。
3. 输入成员邮箱。
4. 选择 **Member** 或 **Admin**。
5. 点击 **Send invites**。
6. 对方打开邀请邮件并加入组织。
7. 回到 Members 页面，确认成员和角色正确。

### 最小权限建议

- 普通开发者先给 Member。
- 只有负责账单、集成、Secrets 或组织设置的人才给 Admin。
- 成员离职或不再参与项目时，及时移除。
- Devin 组织权限和 GitHub 仓库权限要分别管理；加入 Devin 组织不等于自动获得所有仓库权限。

---

## 12.3 导入本地 VS Code 设置

Devin IDE 基于 VS Code。你可以同步熟悉的：

- Settings。
- Extensions。
- Keyboard Shortcuts。

这样在 Devin IDE 中接手工作时，不需要重新安装常用扩展和调整快捷键。

### 方式一：自动导入（推荐）

1. 打开一个由你创建的 Devin Session。
2. 进入 Devin IDE 的 Welcome 页面。
3. 如果 Welcome 没有出现：
   - macOS 按 `Cmd+Shift+P`。
   - Windows / Linux 按 `Ctrl+Shift+P`。
   - 搜索并运行 `Help: Welcome`。
4. 点击导入设置的按钮。
5. 按页面提示，把提供的 Python 脚本复制到你**本地电脑**的终端运行。
   - Windows 使用 PowerShell。
6. 脚本会要求你打开一个 `app.devin.ai` 链接确认。
7. 确认后，设置会同步到当前和未来 Session。

> 只运行 Devin 官方页面在当前导入流程中生成的脚本。不要运行聊天、邮件或陌生网页提供的脚本。

### 方式二：手动导出和导入

自动导入失败时：

1. 在本地 VS Code 打开命令面板。
2. 运行 `Preferences: Open Profiles (UI)`。
3. 右键常用 Profile，选择 **Export**。
4. 导出为 `.code-profile` 文件。
5. 回到 Devin IDE，打开命令面板。
6. 运行 `Preferences: Devin: Import Profile (Manual)`。
7. 选择 `.code-profile` 文件并等待安装。

### 如何验证？

- 运行 `Preferences: Open User Settings (JSON)`，核对设置。
- 运行 `Preferences: Open Keyboard Shortcuts (JSON)`，核对快捷键。
- 打开 Extensions 面板，确认常用扩展已安装。

设置会在你后续的 Session 之间自动同步。

---

## 12.4 Auto-triage：自动调查和分流 Slack Bug

### 它是什么？

Auto-triage 是一种特殊 Automation。一个长期运行的父 Devin 持续监听 Slack 频道：

1. 过滤普通聊天和噪声。
2. 判断消息是否是 bug、回归或事故。
3. 识别重复报告。
4. 为需要调查的问题启动子 Devin。
5. 子 Devin 查看代码和运行时信息，分析根因。
6. 在 Slack 线程中回复诊断结果，并提醒合适的代码负责人。

它还有一个持久化 **Scratchpad（草稿记忆）**，用来记录最近处理的问题、重复项和代码负责人映射。

### 使用前准备

- 已连接个人 Slack：**Settings → Connections → Slack**。
- 已把 Devin 邀请进专门的 bug 或 incident 频道。
- Devin 已连接相关代码仓库。
- 推荐连接 Sentry、Datadog、Linear 等 MCP，让 Devin 能查看错误、日志、指标和工单。

### 创建步骤

1. 打开 **Automations**。
2. 选择 **Triage bug reports on Slack** 模板。
3. 选择要监听的 Slack 频道。
4. 编写 Setup Prompt，说明：
   - 哪类问题要调查。
   - 哪类消息要忽略。
   - 哪些目录或服务由谁负责。
   - 回复中必须包含什么证据。
5. 设置 ACU 和触发次数上限。
6. 保存并启用。

Setup Prompt 示例：

```text
重点调查支付服务的回归问题。
只有消息包含复现步骤、错误日志或 Sentry 链接时才深入调查；
信息不足时先在线程中提问。
找到根因后，给出相关源码文件、证据和建议负责人。
不要自动修改生产环境。
```

### 新手上线方式

不要第一天就监听整个公司 Slack：

1. 先选一个低流量的 `#bugs-test` 频道。
2. 用 5-10 条已知问题测试判断和去重是否正确。
3. 纠正 Devin 标错的负责人，它会更新 Scratchpad。
4. 确认消耗和回复质量后，再扩大范围。

### 安全提醒

Slack 消息属于不可信输入，可能包含恶意链接或提示词。生产场景应配置 Automation 的网络策略，限制不必要的外部访问。

---

## 12.5 Deployments：Devin 能部署什么？

### 先记住结论

Devin 的部署能力主要面向它**从零创建的小型应用**，不是通用生产发布平台。

| 场景 | 官方支持情况 |
|------|--------------|
| Devin 用官方前端模板从零创建的小应用 | 获得用户明确批准后，可以部署 |
| Devin 用 FastAPI 模板从零创建的后端 | 获得用户明确批准后，可以部署到 Fly.io |
| 已有前端项目 | 需要你提供部署平台、权限、Secrets 和准确流程，不保证开箱即用 |
| 已有后端或复杂生产系统 | 不应假设 Devin 能直接部署；必须使用项目自己的发布流程 |

### 为什么已有项目更难？

真实项目通常还依赖：

- 云平台账号和最小权限凭证。
- DNS、证书和域名。
- 数据库迁移。
- CI/CD、容器镜像和基础设施。
- Staging / Production 审批和回滚规则。
- 公司内部网络和合规要求。

这些信息不会自动出现在代码仓库中。

### 正确做法

如果要让 Devin 协助已有项目部署：

1. 把经过验证的部署命令写入 Knowledge、AGENTS.md 或 Skill。
2. 用 Secrets 提供专用、最小权限的部署凭证。
3. 明确环境：Preview、Staging 还是 Production。
4. 明确验证方法和回滚命令。
5. 先在非生产环境演练。
6. 涉及公开部署、付费资源或生产修改时，要求 Devin 先请求批准。

推荐提示词：

```text
先不要部署。
请阅读仓库的部署文档和 CI 配置，列出：
1. 需要的凭证；
2. 将运行的命令；
3. 会修改的外部资源；
4. 验证和回滚步骤。
等我确认后，只部署到 staging。
```

---

## 12.6 Autofix Settings：处理机器人评论

### 默认为什么忽略 Bot？

当 Devin 跟踪一个 PR 时，它会监听新评论。默认情况下，它忽略 Bot 评论，避免出现无限循环：

```text
Review Bot 评论
→ Devin 修改代码
→ Review Bot 再评论
→ Devin 再修改
→ 一直循环并持续消耗
```

### 在哪里配置？

只有组织管理员可以修改：

**Settings → Customization → Pull request settings → Autofix settings - bot comments**

### 三种模式

| 模式 | 说明 | 建议 |
|------|------|------|
| Don't respond to bot comments | 忽略所有 Bot 评论 | 默认且最安全 |
| Respond to all bot comments | 处理所有 Bot 评论 | 风险最高，不建议新手使用 |
| Respond to specific bots only | 只处理白名单 Bot | 大多数团队推荐 |

### 推荐设置方法

1. 选择 **Respond to specific bots only**。
2. 输入可信 Bot 的 GitHub 用户名，例如 `github-actions[bot]`。
3. 点击 **Add**。
4. 一次只添加一个 Bot。
5. 观察几个 PR，确认没有循环后再增加。

如果希望 Session 自动处理 Devin Review 的有效发现，把下面账号加入白名单：

```text
devin-ai-integration[bot]
```

“No Issues Found”之类没有实际问题的总结不会触发修复。

### 和 mention-only 的关系

如果同时开启了“只响应提到 Devin 的 PR 评论”，Bot 评论还必须以 `DevinAI` 或 `@devin` 提到 Devin，才会继续处理。

### 特殊情况

包含 `lint check failed` 的 Bot 评论会始终被处理，这样 Devin 仍然可以响应 lint CI 失败。

### 出现循环怎么办？

1. 立即切回 **Don't respond to bot comments**。
2. 暂停 PR 评论监控。
3. 检查是哪个 Bot 反复产生新评论。
4. 改用白名单，并降低自动修复范围。

---

## 12.7 产品指南覆盖索引

| 官方主题 | 本教程位置 |
|----------|------------|
| Knowledge | 第五章、11.3 |
| Skills | 11.3 |
| Session Insights | 11.12 |
| Secrets 与 Site Cookies | 11.4 |
| 创建 Playbooks | 5.2 |
| 使用 Playbooks | 5.2 |
| Automations | 11.8 |
| Auto-triage | 12.4 |
| Scheduled Sessions | 6.5、11.8 |
| Deployments | 12.5 |
| Autofix Bot Comments | 12.6 |
| 邀请团队 | 8.3、12.2 |
| 导入本地设置 | 12.3 |

---

## 官方参考

- Knowledge：https://docs.devin.ai/product-guides/knowledge
- Skills：https://docs.devin.ai/product-guides/skills
- Session Insights：https://docs.devin.ai/product-guides/session-insights
- Secrets：https://docs.devin.ai/product-guides/secrets
- Creating Playbooks：https://docs.devin.ai/product-guides/creating-playbooks
- Using Playbooks：https://docs.devin.ai/product-guides/using-playbooks
- Automations：https://docs.devin.ai/product-guides/automations
- Auto-triage：https://docs.devin.ai/product-guides/auto-triage
- Scheduled Sessions：https://docs.devin.ai/product-guides/scheduled-sessions
- Deployments：https://docs.devin.ai/product-guides/deployment-capabilities
- Autofix Bot Comments：https://docs.devin.ai/product-guides/bot-comment-settings
- Invite your Team：https://docs.devin.ai/product-guides/invite-team
- Importing Code Settings：https://docs.devin.ai/collaborate-with-devin/vscode-profiles

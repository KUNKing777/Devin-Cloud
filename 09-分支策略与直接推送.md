# 第九章：Devin 的分支策略 —— PR 工作流 vs 直接推送（main / init 的区别）

> 这是很多新手最容易搞糊涂的地方："Devin 到底把代码放哪了？""它在 main 上工作和在 init 上工作有什么不一样？""为什么有时候有 PR，有时候没有 PR？"
> 本章用最直白的话、配合每一步的操作，把这件事彻底讲清楚。

---

## 9.1 先记住一个核心区别

Devin 提交代码有**两种完全不同的模式**：

| 模式 | Devin 做了什么 | 有没有 PR | 谁来把关 |
|------|----------------|-----------|----------|
| **PR 工作流（默认 / 推荐）** | 新建一个分支 `devin/xxx`，把代码推到这个新分支，再开一个 Pull Request | 有 PR | 你在 GitHub 上 review 后才合并 |
| **直接推送（需手动配置）** | 直接把代码 commit + push 到某个已存在的分支（比如 `main` 或 `init`），不开 PR | 没有 PR | 没有把关，推上去就生效 |

> 一句话记忆：
> - **有 PR = 安全模式**，代码先进"待审区"，你点了同意才进主线。
> - **直接推送 = 快速模式**，代码直接落到分支上，适合个人小项目或你完全信任 Devin 的场景。

**默认情况下，Devin 走的是 PR 工作流**（第四章讲的就是这种）。只有当你**明确配置**了"直接推送到某分支、不要开 PR"时，Devin 才会走直接推送模式（本章 9.4 节讲怎么配）。

---

## 9.2 名词扫盲（新手必看）

在继续之前，先把几个词搞清楚，否则后面会越看越乱：

- **分支（Branch）**：可以理解成代码的"平行存档"。`main` 是主存档，你可以从它复制出 `init`、`dev`、`devin/xxx` 等其它存档，各自独立修改，互不影响，最后再合并回去。
- **main 分支**：仓库的**默认分支 / 主分支**，通常代表"正式、可用"的代码。新建仓库时 GitHub 默认给你的就是它（老仓库可能叫 `master`）。
- **init 分支**：**没有任何特殊含义**，它只是一个"名字叫 init 的普通分支"。有人喜欢用它当"Devin 专用工作分支"或"初始化/开发分支"。它和 `dev`、`feature-x` 没有本质区别，关键看你团队怎么约定。
- **base 分支（基础分支 / 目标分支）**：Devin 从哪个分支"长出"新分支，以及 PR 最终要合并回哪个分支。默认是 `main`。
- **commit（提交）**：把一次代码改动"存档"，附带一句说明。
- **push（推送）**：把本地的 commit 上传到 GitHub。
- **PR（Pull Request，拉取请求）**：一个"申请：把我这个分支的改动合并到目标分支"的请求页面，是 GitHub 上 review 代码的地方。

---

## 9.3 模式一：PR 工作流（默认行为，最常见）

这是 Devin 不做任何特殊配置时的默认行为，也是**最推荐新手使用的方式**。

### 9.3.1 Devin 在 main 上工作时（base = main）

注意这里的措辞："在 main 上工作"≠"直接改 main"。默认情况下 Devin **不会**直接动 main，它只是**以 main 为基础**：

```
main ─────●─────────────────────●──────  ← 你 merge 后，改动才进入 main
           \                   /
            ●──●──●──●──●        ← Devin 的新分支 devin/<时间戳>-<功能>
            （Devin 在这里写代码、commit、push）
```

完整流程（每一步对应你看到的画面）：

1. Devin 在云端虚拟机里 `git checkout main && git pull`（拿到最新的 main）。
2. Devin 执行类似 `git checkout -b devin/1717750000-add-login`（从 main 长出一个新分支）。
3. Devin 在新分支上改代码、跑 lint/test、`git commit`、`git push`。
4. Devin 自动开一个 **PR，目标分支（base）是 `main`**。
5. session 里出现 **PR 链接** → 你点进去 → 在 GitHub 上 review（见第四章）。
6. 你点 **Merge** → 改动这才正式进入 `main`。

**关键点：在你点 Merge 之前，main 分支一个字都没变。** 这就是 PR 工作流的安全感来源。

### 9.3.2 Devin 在 init（或其它分支）上工作时（base = init）

如果你告诉 Devin "基于 init 分支工作"，唯一的区别是：**新分支从 init 长出，PR 的目标分支变成 init**。

```
init ─────●─────────────────────●──────  ← 你 merge 后，改动进入 init（不是 main！）
           \                   /
            ●──●──●──●──●        ← Devin 的新分支 devin/<时间戳>-<功能>
```

流程完全一样，只是把上面所有的 `main` 换成 `init`：
1. Devin `git checkout init && git pull`。
2. 从 init 长出 `devin/xxx` 新分支。
3. 改代码、commit、push。
4. 开 PR，**目标分支是 `init`**。
5. 你 review、merge → 改动进入 `init`，**main 完全不受影响**。

### 9.3.3 怎么让 Devin 基于 init（而不是 main）工作？

在任务描述里**明确写清楚 base 分支**即可，例如：

```
请基于 init 分支工作（不要用 main）：
从 init 创建新分支，完成后把 PR 的目标分支设为 init。

任务：在 src/login.ts 中添加记住登录状态的功能。
```

或者更简洁：

```
@Repos my-repo（base 分支用 init）

任务：……
```

> 💡 小白提示：如果你不特别说明，Devin 默认用仓库的**默认分支**（一般是 main）。想用别的分支，**一定要在 prompt 里写明**，否则 Devin 不会猜。

### 9.3.4 PR 工作流下，"base = main" vs "base = init" 的区别总结

| 对比项 | base = main | base = init |
|--------|-------------|-------------|
| 新分支从哪长出来 | main | init |
| PR 合并后改动进入 | main（主线，通常代表正式代码） | init（一个中间分支，main 不动） |
| 风险 | 合并即影响主线，需认真 review | 改动先进 init，main 有"缓冲带"，更安全 |
| 典型用途 | 直接面向主线开发的小改动 | 想先在 init 上攒一批改动，确认 OK 后再统一合并到 main |
| 是否仍然有 PR/review | 有 | 有 |

---

## 9.4 模式二：直接推送（不开 PR）—— 高级 / 个人项目

有些人（尤其是个人项目、快速迭代）**不想每次都开 PR、点 merge**，希望 Devin "改完直接推上去就行"。这就是**直接推送模式**。

> ⚠️ **重要提醒**：直接推送 = **没有 review 把关，改动推上去立刻生效**。
> - 适合：个人项目、原型、你完全信任 Devin 的场景。
> - 不适合：团队协作、生产环境、重要仓库。团队场景请老老实实用 PR 工作流 + 分支保护（见第四章 4.5 和第八章）。

### 9.4.1 怎么配置"直接推送、不开 PR"？

最常用、最稳的方式是**添加一条 Knowledge**（参考第五章），告诉 Devin 你的偏好。例如：

**直接推送到 main：**

```
Trigger Description（触发描述）：
当在 my-repo 仓库工作时

Content（内容）：
以后所有代码改动都直接 commit 并 push 到 main 分支，不要开 PR、
不要新建分支（除非我当次明确要求）。push 前先 git checkout main && git pull，
改动通过编译/lint/单测后再推送；回滚用 git revert，不要对 main 强推（force push）。
```

**直接推送到 init：**

```
Content（内容）：
以后所有代码改动都直接 commit 并 push 到 init 分支，不要开 PR、
不要新建分支（除非我当次明确要求）。push 前先 git checkout init && git pull，
改动通过编译/lint/单测后再推送；回滚用 git revert，不要对 init 强推。
```

> 你也可以不写 Knowledge，而是**每次在任务描述里临时说明**：
> ```
> 这次改完直接 commit 并 push 到 init 分支，不要开 PR、不要新建分支。
> ```
> 区别：写成 Knowledge 是"长期生效、每次自动记住"；写在 prompt 里是"只对这一次生效"。

### 9.4.2 直接推送到 main vs 直接推送到 init —— 到底差在哪？

这正是你最关心的问题。两者**流程一模一样**（都是 commit + push，不开 PR），区别只在于**改动落到了哪条分支、影响有多大**：

```
直接推送到 main：
main ──●──●──●──●──●──  ← Devin 每次都直接往主线上落代码
                         （没有缓冲，推上去就是"正式代码"）

直接推送到 init：
main ──●───────────────  ← main 纹丝不动，保持干净
        \
init ────●──●──●──●──●──  ← Devin 的改动都堆在 init 上
                         （等你确认 OK，再把 init 合并回 main）
```

| 对比项 | 直接推送到 **main** | 直接推送到 **init** |
|--------|--------------------|---------------------|
| 改动落到哪 | 主分支（通常是正式/默认分支） | init 这条独立分支 |
| main 会不会变 | **会，立即变** | **不会**，main 始终干净 |
| 风险等级 | 高（推错了立刻影响主线） | 中（错了也只脏了 init，main 可控） |
| 出问题怎么救 | 只能在 main 上 `git revert`（历史会留痕） | 直接修 init，或干脆丢弃 init 重建，main 不受波及 |
| 什么时候进 main | 一推就在 main | 需要你**手动**把 init 合并/提 PR 到 main |
| 适合谁 | 个人玩具项目、极快迭代、你不在乎主线被频繁改动 | 想要"Devin 随便推、但 main 必须稳"的人 |

**一句话总结：**
- **推 main** = 没有安全网，快但险。
- **推 init** = 给 Devin 一个"沙盒分支"随便折腾，main 始终是你手动把关后才更新的"安全区"。

### 9.4.3 直接推送模式下，你在 GitHub 上怎么操作？

因为**没有 PR**，所以你不是去 "Pull requests" 页面 review，而是直接去看**分支上的提交记录**：

#### 查看 Devin 推了什么

1. 打开你的 GitHub 仓库页面。
2. 左上角有一个**分支下拉框**（默认显示 `main`），点击它，选择 Devin 推送的分支（`main` 或 `init`）。
3. 点击仓库文件列表上方的 **Commits**（提交记录，通常显示成"🕐 xx commits"）。
4. 你会看到 Devin 的每一次提交，点进任意一条 commit 就能看到它具体改了哪些代码（绿色新增 / 红色删除）。

#### 如果发现 Devin 推错了，怎么"撤销"？

直接推送没有"不合并"这个选项（因为已经推上去了），所以撤销靠 `git revert`（生成一个反向提交来抵消，而不是抹掉历史）：

**方式 A：让 Devin 撤销（推荐新手）**

新开一个 session，告诉它：
```
请用 git revert 撤销 init 分支上最近一次提交（commit xxxxxxx），
然后 push 回 init。不要 force push。
```

**方式 B：自己在本地撤销**
```bash
# 以 init 为例
git checkout init
git pull origin init
git log --oneline          # 找到要撤销的那条 commit 的 id
git revert <commit-id>     # 生成一个反向提交
git push origin init       # 推回去
```

> ❌ **千万不要**用 `git push --force` / `git reset --hard` 去强行覆盖 main 或 init，尤其是团队仓库 —— 这会抹掉别人的提交，造成灾难。用 `git revert` 永远是安全的。

---

## 9.5 我该选哪种模式？（决策建议）

| 你的情况 | 推荐模式 | 推荐 base 分支 |
|----------|----------|----------------|
| 团队协作 / 公司仓库 / 生产代码 | **PR 工作流** | main（并开启分支保护） |
| 多人共管一个仓库 | **PR 工作流** | main（见第八章） |
| 个人项目，但想保留 review 习惯 | PR 工作流 | main 或 init |
| 个人玩具项目，追求极速、懒得 review | 直接推送 | init（给自己留个安全区） |
| 个人项目，主线随便改无所谓 | 直接推送 | main |

> 🟢 **给小白的最稳建议**：先用**默认的 PR 工作流（base = main）**，把第四章的 review 流程走熟。等你完全清楚自己在做什么、且是个人项目时，再考虑切换到直接推送模式。

---

## 9.6 常见疑问

### Q: 我没配置任何东西，Devin 会直接改我的 main 吗？

**不会**。默认是 PR 工作流：Devin 新建 `devin/xxx` 分支并开 PR，在你点 Merge 之前 main 不会变。（前提是你没有手动配置"直接推送"，也没有在 prompt 里要求它直接推。）

### Q: 我配置了"直接推送到 main"，但又想偶尔走 PR，怎么办？

在那一次的任务描述里临时覆盖即可，例如："这次请按正常 PR 流程：新建分支、开 PR，不要直接推 main。" 当次的明确指令会**优先于** Knowledge 里的长期偏好。

### Q: init 分支不存在，Devin 能用吗？

如果 init 分支还不存在，先在 GitHub 上创建它：进入仓库 → 点分支下拉框 → 在输入框输入 `init` → 点 **Create branch: init from 'main'**。或者直接让 Devin "从 main 创建 init 分支并推送"。

### Q: 直接推送到 main，会触发 CI 吗？

会。CI（如 GitHub Actions）通常配置成"push 到任何分支都跑"或"push 到 main 跑"。直接推送同样会触发 CI，只是**没有 PR 页面那种"合并前必须通过"的拦截**——代码已经在分支上了，CI 只是事后告诉你红还是绿。

### Q: main 上有分支保护，Devin 还能直接推 main 吗？

**不能**，而且这正是分支保护的意义。如果你开启了"Require a pull request before merging"，那么任何人（包括 Devin）都无法直接 push 到 main，只能走 PR。这时即使你配了"直接推送 main"，推送也会被 GitHub 拒绝。想直接推送，要么去掉保护（不推荐），要么改成直接推送到一个**没有保护的分支**（如 init）。

---

## 9.7 小结

1. Devin 有两种提交模式：**PR 工作流（默认、安全）** 和 **直接推送（需配置、快但险）**。
2. **PR 工作流**里，"base = main" 和 "base = init" 的区别只是 PR 合并回哪条分支；两者都有 review 把关。
3. **直接推送**里，"推 main" 和 "推 init" 的区别是**改动落到主线还是落到沙盒分支**：推 main 没有安全网，推 init 给 main 留了缓冲带。
4. 团队 / 生产 → 永远用 PR 工作流 + 分支保护；个人玩具项目才考虑直接推送。
5. 撤销改动一律用 `git revert`，**绝不**对 main/init 强推（force push）。

下一步：如果你还没读第四章，强烈建议回去把 PR 的逐步 review 操作走一遍——那是你日常用得最多的流程。

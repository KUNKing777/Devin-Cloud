# 第七章：常见问题与故障排除

> 使用 Devin 过程中可能遇到的各种问题和解决方案。

---

## 7.1 注册与账号问题

### Q: 我用 GitHub 注册后，看不到我的仓库怎么办？

**原因**：Devin 需要你明确授权访问具体仓库。

**解决步骤**：
1. 进入 GitHub → 你的组织 Settings → GitHub Apps
2. 找到 Devin.ai 集成，点击 Configure
3. 在 Repository access 中，选择你要授权的仓库
4. 保存

或者在 Devin 中：
1. Settings → Integrations → GitHub
2. 检查连接状态
3. 如果断开了，重新连接

### Q: 我不是 GitHub 组织的管理员，能连接仓库吗？

**不能直接连接**。你需要：
- 让组织管理员在 GitHub 上安装 Devin 集成
- 或者让管理员给你管理员权限

### Q: Free 方案有什么限制？

Free 方案限制：
- 有限的 ACU（Agent Compute Unit）配额
- 不能使用某些高级模型
- 有并发 session 数量限制
- 无限 Tab 补全和内联编辑

---

## 7.2 环境配置问题

### Q: Devin 构建快照失败了怎么办？

**解决步骤**：
1. 进入 Settings → Devin's Machine → Version History
2. 回滚到之前成功的版本
3. 检查你修改的配置是否有错误
4. 在 Devin's Machine 的终端中手动测试命令
5. 修复后重新保存

### Q: Devin 运行时找不到依赖怎么办？

**可能原因**：
- 依赖安装命令不正确
- 依赖安装在了错误的目录
- 环境变量没有正确配置

**解决步骤**：
1. 进入 Devin's Machine 的终端
2. 手动运行依赖安装命令
3. 检查是否成功
4. 如果需要特定目录，使用 `cd <目录> && <命令>`

### Q: Devin 不知道如何运行我的项目？

**解决方法**：
1. 在环境配置的 **Additional Notes** 步骤中，详细说明如何运行
2. 或者在 Knowledge 中添加一条知识，说明项目的运行方式

---

## 7.3 Session 问题

### Q: Devin 的 session 卡住了怎么办？

**解决步骤**：
1. 等待几分钟，Devin 可能在处理复杂任务
2. 如果确实卡住了，点击 **Stop** 按钮终止 session
3. 创建一个新的 session，重新提交任务
4. 如果问题持续，检查任务描述是否清晰

### Q: Devin 做的结果不是我想要的怎么办？

**原因**：任务描述不够清晰。

**解决方法**：
1. 在当前 session 中给 Devin 反馈，告诉它哪里不对
2. 或者终止 session，重新写一个更清晰的任务描述
3. 添加 Knowledge 记住这次的教训

### Q: Devin 没有运行测试就提交了怎么办？

**可能原因**：
- 环境配置中没有设置 test 命令
- Devin 认为测试不适用于这个任务

**解决方法**：
1. 在 prompt 中明确要求："提交前必须运行 npm test 并确保通过"
2. 在 Knowledge 中添加："所有代码修改必须通过测试才能提交"
3. 在 Playbook 中加入测试步骤

### Q: Devin 直接 push 到了 main 分支怎么办？

**原因**：你的仓库没有设置分支保护规则。

**解决方法**：
1. 立即在 GitHub 上设置分支保护规则（见第四章 4.5 节）
2. 在 Knowledge 中添加："禁止直接 push 到 main 分支，必须通过 PR"
3. 在 Playbook 的 Forbidden Actions 中加入这条规则

---

## 7.4 PR 相关问题

### Q: Devin 的 PR 有冲突怎么办？

**解决步骤**：
1. 在 PR 评论中告诉 Devin："这个 PR 有冲突，请解决"
2. Devin 会尝试 rebase 或 merge 来解决冲突
3. 如果冲突太大，Devin 可能会告诉你需要手动处理

### Q: Devin 不回复我的 PR 评论怎么办？

**可能原因**：
- Session 已经被归档（archived）
- Devin 的 session 已经超时

**解决方法**：
1. 创建一个新的 session
2. 告诉 Devin："请处理 PR #123 上的 review 评论"
3. 提供 PR 链接

### Q: CI 检查失败了，Devin 不自动修复怎么办？

**解决方法**：
1. 在 PR 评论中 @Devin，告诉它 CI 失败了
2. 提供失败的 CI 日志链接
3. Devin 会分析失败原因并修复

### Q: 如何让 Devin 的 PR 自动合并？

**步骤**：
1. 在 PR 页面，点击 Merge 按钮旁边的下拉箭头
2. 选择 **Enable auto-merge**
3. 选择合并方式（Squash / Merge / Rebase）
4. 当所有 CI 检查通过后，PR 会自动合并

---

## 7.5 Devin Review 问题

### Q: Devin Review 不工作怎么办？

**检查清单**：
1. 是否连接了 GitHub App（不是 PAT）
2. 是否有仓库的访问权限
3. 是否在 Settings → Review 中配置了触发模式

### Q: Auto-Review 没有触发怎么办？

**检查清单**：
1. 进入 Settings → Preferences → Devin Review
2. 确认 Review trigger 不是 Manual
3. 检查仓库是否在 auto-review 列表中
4. 确认 PR 不是草稿（draft）

### Q: Devin Review 消耗太多 ACU 怎么办？

**解决方法**：
1. 进入 Settings → Review
2. 设置 per-PR auto-review spend limit
3. 将不重要的仓库从 auto-review 列表中移除
4. 将触发模式改为 "On PR creation"（只在创建时审查一次）

---

## 7.6 团队协作问题

### Q: 团队成员看不到我的 Knowledge 怎么办？

**检查**：
1. Knowledge 的 Scope 是否设置为 Organization（不是 Personal）
2. 团队成员是否在同一个 Organization 中

### Q: 如何让 Devin 区分不同团队成员的任务？

**方法**：
1. 在 prompt 中明确说明你是谁、你的角色
2. 使用 @Sessions 引用之前的 session
3. 在 Knowledge 中添加团队成员的角色和职责

### Q: Managed Devins 的子任务互相冲突怎么办？

**原因**：子任务修改了相同的文件。

**解决方法**：
1. 在拆分任务时，确保子任务之间没有文件重叠
2. 或者让 Devin 按顺序执行，而不是并行

---

## 7.7 费用相关问题

### Q: 什么是 ACU？

ACU（Agent Compute Unit）是 Devin 的计费单位。所有 Devin 产品（sessions、Review、API）都消耗 ACU。

### Q: 如何查看 ACU 消耗？

1. 进入 Settings → Consumption
2. 查看总消耗和按产品分类的消耗
3. 查看按用户分类的消耗

### Q: 如何控制 ACU 消耗？

**方法**：
1. 给 Devin 清晰的任务描述，减少无效尝试
2. 使用 Fast Mode 处理简单任务
3. 设置 per-PR Review 消耗上限
4. 定期审查 ACU 消耗报告

---

## 7.8 通用排错步骤

当 Devin 出现问题时，按以下顺序排查：

1. **检查任务描述** — 是否足够清晰和具体？
2. **检查环境配置** — 依赖是否正确安装？命令是否正确？
3. **检查 Knowledge** — 是否有冲突或过时的知识？
4. **检查 GitHub 连接** — 集成是否正常？权限是否足够？
5. **查看 session 日志** — 在 Shell 标签页中查看 Devin 执行了什么命令
6. **重启 session** — 有时候重新开始就能解决问题
7. **查看官方文档** — https://docs.devin.ai
8. **联系支持** — support@cognition.ai

---

## 7.9 小结

遇到问题时：
1. 先检查最基本的配置（GitHub 连接、环境配置）
2. 再检查任务描述是否清晰
3. 利用 Devin 的日志功能排查
4. 善用 Knowledge 和 Playbook 避免重复问题
5. 必要时联系官方支持

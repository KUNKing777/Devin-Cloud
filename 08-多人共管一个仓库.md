# 第八章：多个个人开发者共管一个仓库

> 不用 Teams 方案，几个人各自用 Free/Pro 账号协作同一个仓库的完整指南。

---

## 8.1 问题背景

常见场景：
- 2-5 个小团队，不想花 $80/月 Teams 方案的钱
- 每人各自注册了 Devin Free 或 Pro 账号
- 需要多人协作同一个 GitHub 仓库
- 但 Devin 的 Knowledge、Playbook、Secrets 都是账号隔离的

本章教你如何在不用 Teams 方案的情况下，实现多人协作。

---

## 8.2 方案一：各自独立 + GitHub 协调（最简单，推荐起步）

### 核心思路

每人各自用自己的 Devin 账号，协调机制靠 GitHub 本身，不靠 Devin。

### 每个人各自做的操作

#### 第一步：注册 Devin 账号

每个人各自打开 https://app.devin.ai 注册（Free 或 Pro）。

#### 第二步：连接同一个 GitHub 仓库

每个人各自操作：

1. 打开 https://app.devin.ai
2. 进入 **Settings** → **Integrations** → **GitHub**
3. 点击 **Add Connection**
4. 选择同一个 GitHub 组织或个人账号
5. 授权访问同一个仓库
6. 确认连接状态为 **Connected**

#### 第三步：各自配置环境

每个人各自配置 Devin 的开发环境（参考第二章）。

注意：每个人的 Devin 快照是独立的，需要各自配置一次。

### 协作流程

```
你的 Devin → 创建分支 devin/xxx-task-A → 提 PR
同事的 Devin → 创建分支 devin/yyy-task-B → 提 PR

两个 PR 互不干扰（不同分支），在 GitHub 上各自 review 各自的。
```

### 注意事项

- **提前约定分工**：避免两人让 Devin 改同一个文件
- **用 GitHub Issues 记录分工**：谁做什么任务，一目了然
- **合并顺序**：后合并的 PR 可能需要解决冲突

---

## 8.3 方案二：共享一个 Devin 账号（最省事，适合 2-3 人）

### 核心思路

只用一个 Devin 账号，多人共享登录。

### 操作步骤

1. 创建一个共享邮箱，比如 `devin@yourteam.com`
2. 用这个邮箱注册 Devin（Pro 方案，$20/月）
3. 把账号密码分享给团队所有人
4. 所有人登录同一个 Devin 账号

### 优点

- **Knowledge 共享** — 一个人添加的知识，所有人都能用
- **Playbook 共享** — 一个人创建的 Playbook，所有人都能用
- **Secrets 共享** — API Key 等只配一次
- **Session 可见** — 所有人都能看到所有 session 的历史
- **省钱** — 只需一个 Pro 账号的费用

### 缺点

- **并发限制** — 同一时间只能有一个活跃 session（Free/Pro 限制）
- **没有权限隔离** — 所有人都能看所有东西
- **无法区分任务归属** — 不知道是谁布置的任务

### 适合场景

- 2-3 人的小团队
- 任务不重叠（不会同时需要两个 Devin session）
- 预算有限

---

## 8.4 方案三：GitHub Organization 统一管理（推荐 3-5 人团队）

### 核心思路

每人各自用自己的 Devin 账号，但通过 GitHub Organization 统一管理仓库和协作流程。

### GitHub 端操作（只需做一次）

#### 第一步：创建 GitHub Organization

1. 打开 https://github.com/organizations/plan
2. 选择 **Free** 方案（免费）
3. 填写 Organization 名称，比如 `your-team-name`
4. 填写联系邮箱
5. 完成创建

#### 第二步：把仓库放到 Organization 下

**方式 A：新建仓库**
1. 在 Organization 下创建新仓库
2. 把代码推上去

**方式 B：转移已有仓库**
1. 打开你的个人仓库
2. 点击 **Settings**
3. 滚动到最底部的 **Danger Zone**
4. 点击 **Transfer ownership**
5. 输入 Organization 名称
6. 确认转移

**方式 C：Fork 已有仓库**
1. 打开仓库页面
2. 点击右上角的 **Fork** 按钮
3. 选择你的 Organization 作为目标

#### 第三步：邀请团队成员

1. 打开 Organization 页面
2. 点击 **People** → **Invite member**
3. 输入同事的 GitHub 用户名或邮箱
4. 选择角色：
   - **Member** — 普通成员，可以推送代码、创建 PR
   - **Owner** — 管理员，可以管理 Organization 设置
5. 发送邀请

#### 第四步：设置分支保护规则

这一步非常重要，防止任何人（包括 Devin）直接 push 到 main：

1. 打开仓库页面
2. 点击 **Settings**
3. 在左侧边栏点击 **Branches**
4. 在 **Branch protection rules** 下，点击 **Add rule**
5. 在 **Branch name pattern** 中输入 `main`
6. 勾选以下选项：
   - ☑ **Require a pull request before merging**
   - ☑ **Require approvals** — 设置至少 1 人 approve
   - ☑ **Require status checks to pass before merging** — 要求 CI 通过
   - ☑ **Require branches to be up to date before merging**
7. 点击 **Create** 保存

#### 第五步：设置 PR Review 要求

在分支保护规则中，你还可以：

- 设置必须有多少人 approve 才能合并
- 指定特定的 reviewer（Code Owners）
- 要求 review 来自 Organization 成员

### Devin 端操作（每个人各自做）

#### 第一步：连接 GitHub Organization

1. 打开 https://app.devin.ai
2. 进入 **Settings** → **Integrations** → **GitHub**
3. 点击 **Add Connection**
4. 选择你的 **GitHub Organization**（不是个人账号）
5. 选择要授权的仓库
6. 安装 GitHub App

注意：你需要是 Organization 的成员或管理员才能连接。

#### 第二步：配置环境

每个人各自配置 Devin 的开发环境（参考第二章）。

#### 第三步：各自开始使用

每个人用自己的 Devin 账号，给 Devin 布置任务。

### 协作流程

```
第 1 步：用 GitHub Issues 分配任务
  - 创建 Issue：[任务A] 添加用户统计 API
  - 指定负责人：@张三

第 2 步：各自让 Devin 处理自己的任务
  - 张三的 Devin：处理 Issue #1（任务A）
  - 李四的 Devin：处理 Issue #2（任务B）

第 3 步：Devin 创建 PR
  - Devin 自动创建分支 devin/xxx-task-A
  - Devin 提交 PR，关联 Issue #1

第 4 步：同事 Review PR
  - 李四在 GitHub 上 review 张三的 PR
  - 检查代码，approve 或 request changes

第 5 步：合并
  - Review 通过后，merge PR
  - Issue 自动关闭
```

---

## 8.5 知识共享的替代方案（不用 Teams 时）

### 方法 1：在仓库中创建共享配置文件

在仓库根目录创建以下文件：

#### AGENTS.md（通用指令）

```markdown
# 项目开发规范

## 编码规范
- 使用 TypeScript，禁止 any
- 所有函数必须有 JSDoc 注释
- 错误处理使用 try-catch，不要吞掉错误

## 测试规范
- 测试文件放在 __tests__ 目录下
- 命名格式：xxx.test.ts
- 覆盖率要求：>80%

## Git 规范
- Commit 格式：[类型] 描述（如 [feat] 添加用户统计接口）
- 禁止 force push
- 禁止直接 push 到 main

## API 规范
- 响应格式：{ code: 0, data: null, msg: "" }
- 错误码：0=成功，1=参数错误，2=未授权，3=服务器错误
```

这个文件会被 Devin Review 自动读取，也可以在 prompt 中引用。

#### REVIEW.md（Review 规则）

```markdown
# Review 指南

## 重点检查
- src/auth/ 目录的改动必须检查安全性
- 数据库迁移文件检查向后兼容性
- API 端点必须有输入验证

## 忽略
- src/generated/ 自动生成文件
- lock 文件除非依赖变更
```

#### .devin/playbooks/（共享 Playbook 目录）

把常用的 Playbook 文件放在这个目录，通过 git 同步：

```
.devin/playbooks/
├── quick-pr.md
├── bug-fix.md
├── feature-dev.md
└── code-migration.md
```

### 方法 2：各自创建 Knowledge 但保持同步

1. 团队约定一个 Knowledge 清单
2. 每个人在自己的 Devin 账号中手动创建相同的 Knowledge
3. 当有人更新了 Knowledge，通知其他人也更新

### 方法 3：在 prompt 中引用仓库文件

在给 Devin 布置任务时，直接引用仓库中的规范文件：

```
请参考 @docs/devin-guidelines.md 中的编码规范，
在 statsController.js 中添加用户统计接口。
```

`@docs/devin-guidelines.md` 会把文件内容注入到 Devin 的上下文中。

---

## 8.6 冲突处理

### 什么时候会冲突？

当两个人的 Devin 同时修改了同一个文件的同一个位置时，后合并的 PR 会有冲突。

### 如何预防？

1. **用 GitHub Issues 分配任务**，明确谁做什么
2. **避免同时修改同一个文件**
3. **大文件拆分**，不同人负责不同部分
4. **频繁合并**，不要攒太多 PR

### 冲突发生后怎么处理？

#### 方法 1：让 Devin 解决

1. 打开有冲突的 PR
2. 在 PR 评论中 @Devin 或创建新 session
3. 告诉 Devin："这个 PR 和 main 分支有冲突，请解决"
4. Devin 会自动 rebase 或 merge 来解决冲突
5. Devin push 新的 commit，冲突解决

#### 方法 2：手动解决

1. 在本地拉取最新代码：
   ```bash
   git checkout main
   git pull origin main
   git checkout devin/xxx-task-A
   git merge main
   ```
2. 手动解决冲突
3. push 解决后的代码：
   ```bash
   git push origin devin/xxx-task-A
   ```

---

## 8.7 各方案对比

| 方案 | 月成本 | 知识共享 | 权限隔离 | 适合规模 | 推荐指数 |
|------|--------|----------|----------|----------|----------|
| 各自独立 | 各自 $0-20 | 无 | 完全隔离 | 2 人 | ★★★ |
| 共享账号 | $20 | 完全共享 | 无 | 2-3 人 | ★★★★ |
| GitHub Org + 各自 Devin | 各自 $0-20 | 需手动同步 | GitHub 层面 | 3-5 人 | ★★★★★ |
| Teams 方案 | $80 + $40/人 | 原生共享 | 完整 | 5 人+ | ★★★★★ |

---

## 8.8 最终推荐方案（2-5 人小团队）

### 第一步：创建 GitHub Organization（免费）

1. 打开 https://github.com/organizations/plan
2. 选择 Free 方案
3. 创建 Organization

### 第二步：仓库迁移到 Organization

把仓库 fork 或 transfer 到 Organization 下。

### 第三步：设置分支保护

1. Settings → Branches → Add rule
2. Branch name pattern: `main`
3. 勾选 "Require a pull request before merging"
4. 勾选 "Require approvals"（至少 1 人）

### 第四步：每人注册 Devin Pro

每人各自注册 Devin Pro（$20/月），连接 Organization 的仓库。

### 第五步：创建共享配置

在仓库中创建：
- `AGENTS.md` — 通用开发规范
- `REVIEW.md` — Review 规则
- `.devin/playbooks/` — 共享 Playbook

### 第六步：建立协作流程

1. 用 GitHub Issues 分配任务
2. 各自让 Devin 处理自己的任务
3. Devin 创建 PR
4. 同事 Review PR
5. Approve 后合并

### 第七步：持续迭代

- 每次 Devin 做得不好，更新 AGENTS.md 或创建新的 Playbook
- 定期同步 Knowledge（各自手动更新）
- 用 Devin Review 辅助代码审查

---

## 8.9 小结

不用 Teams 方案也能协作，核心是：

1. **GitHub Organization** — 统一管理仓库、权限、分支保护
2. **AGENTS.md / REVIEW.md** — 通过 git 同步团队规范
3. **GitHub Issues** — 任务分配和协调
4. **PR Review 流程** — 代码质量保证
5. **各自 Devin 账号** — 独立使用，互不干扰

花最少的钱（每人 $0-20/月），获得最实用的协作能力。
等团队规模到 5 人以上，再考虑升级到 Teams 方案。

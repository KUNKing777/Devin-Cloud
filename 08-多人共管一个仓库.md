# 第八章：多个开发者共管一个仓库

> 本章介绍多人如何安全地让 Devin 操作同一个代码仓库。
> 先说结论：**不要多人共享一个 Pro 或 Max 账号。官方说明 Pro 和 Max 都是单用户方案；多人共享订阅应使用 Teams。**

---

## 8.1 先选协作方式

| 方式 | 适合谁 | 优点 | 限制 |
|------|--------|------|------|
| 每人独立账号 + GitHub 协调 | 临时合作、预算有限的小团队 | 账号和密钥互相隔离 | Knowledge、Playbook、环境配置不会自动共享 |
| Teams 组织 | 正式团队、需要长期协作 | 成员、Knowledge、Playbook、额度和权限统一管理 | 每月最低 $80 |

### 官方账号规则

- Free、Pro、Max 都是个人方案。
- Pro 为 $20/月，Max 为 $200/月，均只供 1 名用户使用。
- Teams 每月最低 $80，可邀请不限数量成员。
- Teams 成员可以是：
  - **Full seat**：$40/月，包含个人使用额度和 Devin Desktop。
  - **Flex seat**：席位本身免费，使用团队共享的 on-demand credits。

价格和规则可能变化，请以官方页面为准：

- https://docs.devin.ai/admin/billing/self-serve
- https://devin.ai/pricing

---

## 8.2 方案一：每人独立账号 + GitHub 协调

### 核心思路

每个人使用自己的 Devin 账号，通过 GitHub Organization、Issue、分支和 PR 协调工作。

```text
张三的 Devin → devin/task-a 分支 → PR A
李四的 Devin → devin/task-b 分支 → PR B

两个人使用不同账号和不同分支，在 GitHub 上互相 review。
```

### 第一步：建立 GitHub Organization

如果仓库已经在 GitHub Organization 下，可以跳过。

1. 打开 https://github.com/organizations/plan
2. 创建一个 GitHub Organization。
3. 把仓库创建、转移或 fork 到 Organization 下。
4. 在 Organization 的 **People** 页面邀请成员。

### 第二步：每个人连接同一个仓库

每位成员分别操作：

1. 打开 https://app.devin.ai
2. 进入 **Settings → Integrations → GitHub**
3. 点击添加连接。
4. 选择团队的 GitHub Organization。
5. 只授权需要 Devin 操作的仓库。
6. 确认仓库出现在 Devin 的仓库选择器里。

如果组织管理员限制了 GitHub App 安装，需要管理员批准。

### 第三步：每个人配置自己的环境

独立账号的环境和 Secrets 默认不会自动共享，因此每个人都要：

1. 为仓库配置开发环境和 Snapshot。
2. 添加自己有权限使用的 Secrets。
3. 确认 lint、test、build 和本地启动命令可运行。

减少重复配置的方法见 [8.5 仓库内共享规则和流程](#85-仓库内共享规则和流程)。

### 第四步：用 Issue 分工

一个 Issue 尽量只交给一个人负责。

```text
Issue #101：增加用户统计 API → 张三
Issue #102：增加统计页面 → 李四
```

每个 Issue 建议包含：

- 背景和目标
- 相关文件或页面
- 验收标准
- 设计图、接口文档或错误日志
- 负责人

### 第五步：每个任务走独立 PR

要求每个 Devin Session：

1. 从最新默认分支创建工作分支。
2. 只处理一个主题。
3. 运行 lint、test、build。
4. 创建 PR。
5. 在 PR 里关联 Issue。

这样即使两个任务并行，也更容易 review 和解决冲突。

---

## 8.3 方案二：使用 Teams 组织

Teams 适合需要长期共享 Devin 能力的团队。

### Teams 能解决什么？

- 邀请和管理团队成员。
- 统一管理组织级 Knowledge 和 Playbook。
- 使用团队共享的 on-demand credits。
- 管理 Full seat 和 Flex seat。
- 统一连接 GitHub、Slack、Linear、MCP 等集成。
- 使用团队级 Devin Review、Automations 和管理能力。

### Full seat 和 Flex seat 怎么选？

| 成员类型 | 推荐席位 |
|----------|----------|
| 每天或每周经常使用 Devin | Full seat |
| 偶尔发起任务或只需要临时使用 | Flex seat |
| 只负责 review，不经常开 Session | 先用 Flex seat 观察用量 |

### 新手配置顺序

1. 管理员开通 Teams。
2. 在 **Settings → Members** 邀请成员并选择席位。
3. 连接 GitHub Organization。
4. 只授权需要的仓库。
5. 配置仓库环境和 Snapshot。
6. 添加组织级 Knowledge、Playbook 和必要 Secrets。
7. 配置 Devin Review 和使用额度限制。
8. 再连接 Slack、Teams、Jira、Linear 或 MCP。

### Teams 仍然需要 GitHub 权限管理

Teams 不会替代 GitHub 的安全设置。你仍然应该：

- 开启默认分支保护。
- 要求 PR 才能合并。
- 要求至少 1 人 approve。
- 要求 CI 通过。
- 使用 CODEOWNERS 管理关键目录的 reviewer。

---

## 8.4 GitHub 端的安全设置

### 设置默认分支保护

GitHub 的界面可能随版本变化，通常在：

```text
仓库 → Settings → Rules / Branches
```

为 `main` 或你的默认分支设置：

- Require a pull request before merging
- Require approvals
- Require status checks to pass
- Require conversation resolution
- 阻止 force push
- 阻止删除受保护分支

### 为什么一定要开？

分支保护能防止人或 Devin：

- 直接把未经 review 的代码推到主分支。
- 在 CI 失败时合并。
- 用 force push 覆盖团队历史。

### 使用 CODEOWNERS

关键目录可以指定固定 reviewer：

```text
# .github/CODEOWNERS
/src/auth/ @security-team
/database/migrations/ @backend-team
```

这样认证和数据库迁移相关 PR 会自动请求对应团队 review。

---

## 8.5 仓库内共享规则和流程

即使每个人使用独立 Devin 账号，也可以通过 Git 仓库共享很多信息。

### 方法一：AGENTS.md

在仓库根目录创建 `AGENTS.md`：

```markdown
# AGENTS.md

## Setup
- 使用 pnpm，不要使用 npm。
- 安装依赖：pnpm install。

## Testing
- 前端改动运行 pnpm lint 和 pnpm test。
- 数据库 schema 变更后运行 pnpm prisma generate。

## Git
- 必须创建 PR，不要直接 push main。
- 禁止 force push。

## Project Rules
- 不要手动修改 generated 文件。
- 认证相关改动需要额外安全检查。
```

AGENTS.md 适合保存仓库长期规则，让不同账号下的 Devin 都能从代码库读取。

### 方法二：Skills

把可重复执行的操作流程写成 Skill：

```text
.agents/skills/test-webapp/SKILL.md
```

示例：

```markdown
---
name: test-webapp
description: Start the web app and verify core flows after frontend changes.
---

## Setup
1. pnpm install
2. pnpm dev

## Verify
1. 打开登录页。
2. 使用测试账号登录。
3. 验证本次改动涉及的页面。
4. 运行 pnpm lint 和 pnpm test。
```

Skill 适合“如何测试”“如何部署”“如何登录”这类明确步骤。

### 方法三：仓库文档

可以建立：

```text
docs/devin-guidelines.md
docs/review-checklist.md
docs/release-process.md
```

然后在任务里明确引用：

```text
请按照 docs/devin-guidelines.md 和 docs/review-checklist.md 完成任务。
```

注意：普通自定义文件不一定会被自动加载，最好在 Prompt、AGENTS.md 或 Skill 中明确引用。

### 方法四：Git-backed Blueprint

把环境配置放在：

```text
.devin/blueprint.yaml
```

这样环境配置可以跟代码一起 review 和同步，避免每个人手工维护不同版本。

### 什么不能直接通过仓库共享？

- 真实密码、Token、API Key
- 浏览器登录 Cookie
- 组织后台的权限配置

这些内容应使用 Devin Secrets、Site Cookies 或对应服务的密钥管理功能，不要提交到 Git。

---

## 8.6 防止多人任务互相冲突

### 拆任务时遵守 3 条规则

1. 不要让两个 Devin 同时改同一个功能。
2. 后端接口和前端页面可以分开，但要先约定接口格式。
3. 大任务拆成有依赖顺序的小 PR，不要让多个 PR 各自重构同一片代码。

### 好的并行拆分

```text
任务 A：新增后端 GET /api/stats 接口
任务 B：为现有 stats API 增加测试
任务 C：接口合并后，再实现前端统计页面
```

### 容易冲突的拆分

```text
任务 A：重构用户模块
任务 B：重构认证模块
任务 C：统一整理所有 API
```

如果三个任务都要改相同公共文件，很容易产生冲突。

---

## 8.7 PR 冲突怎么处理？

### 先判断冲突类型

| 冲突 | 处理方式 |
|------|----------|
| 两边只是改了相邻文案、import、lockfile | 可以让 Devin 自动解决 |
| 两边都重写了同一个核心函数 | 需要负责人决定保留哪种设计 |
| 一个 PR 依赖另一个 PR | 先合并依赖 PR，再更新后续分支 |

### 让 Devin 处理

可以在原 Session 或新 Session 中说：

```text
请把这个 PR 更新到最新 main，并解决冲突。
先说明每个冲突两边分别改了什么。
如果是架构或业务逻辑冲突，不要猜，先问我。
解决后运行相关测试并更新 PR。
```

### 手动处理

```bash
git checkout main
git pull origin main
git checkout devin/task-a
git merge main

# 手动解决冲突后
git add <已解决的文件>
git commit
git push origin devin/task-a
```

不要对共享分支使用 `git push --force`。

---

## 8.8 方案对比

| 方案 | 费用 | 团队知识共享 | 权限隔离 | 推荐场景 |
|------|------|--------------|----------|----------|
| 每人 Free | 每人 $0 | 主要靠仓库文件 | 账号完全隔离 | 学习、轻度临时协作 |
| 每人 Pro | 每人 $20/月 | 主要靠仓库文件 | 账号完全隔离 | 独立开发者长期协作 |
| Teams | 最低 $80/月 | 原生共享 | 可管理成员和席位 | 正式团队 |
| 共享一个 Pro/Max 账号 | 不应使用 | 表面上共享 | 无法审计、无权限隔离 | 官方方案不支持 |

---

## 8.9 给 2-5 人团队的推荐

### 预算非常有限、使用很少

1. 每个人使用自己的 Free 或 Pro 账号。
2. 建立 GitHub Organization。
3. 开启分支保护。
4. 使用 Issue 分工。
5. 使用 AGENTS.md、Skills、仓库文档和 Git-backed Blueprint 共享规则。
6. 不共享账号和密码。

### 正式项目或经常使用

1. 使用 Teams。
2. 高频成员分配 Full seat。
3. 低频成员使用 Flex seat。
4. 统一管理 Knowledge、Playbook、Secrets 和集成。
5. 设置团队额度和 Review 规则。
6. 仍然使用 GitHub PR、CI 和分支保护。

---

## 8.10 小结

1. Pro 和 Max 是单用户方案，**不要多人共享账号**。
2. 临时协作可以每人使用独立账号，通过 GitHub 和仓库文件协调。
3. 正式团队优先使用 Teams，最低费用为 $80/月。
4. AGENTS.md 共享长期规则，Skills 共享操作流程，Git-backed Blueprint 共享环境配置。
5. Secrets 不进入 Git，主分支必须有保护。
6. 每个任务只做一个主题，每个主题走独立 PR。

# 第二章：配置 Devin 的开发环境

> 这是让 Devin 真正能工作的关键一步。你可以把它想象成：给新来的同事配一台电脑，装好开发工具。

---

## 2.1 为什么要配置环境？

Devin 运行在一个**云端虚拟机**里（Ubuntu 22.04 系统）。每次启动一个新的 session，Devin 都是从一个**快照（Snapshot）**启动的。

快照 = 一台预装好所有工具和代码的虚拟机镜像。

如果你不配置环境，Devin 就像一个没装任何开发工具的空电脑 — 它没法编译代码、运行测试、甚至没法安装依赖。

---

## 2.2 两种配置方式

### 方式一：让 Devin 自己配置（推荐新手）

最简单的方式：直接让 Devin 帮你配置。

1. 在 Devin 主界面，点击 **New Session**（新建会话）
2. 选择 **Agent** 模式
3. 在输入框中输入：

```
帮我配置这个仓库的开发环境。分析代码结构，安装所有依赖，
配置好 lint 和 test 命令。
```

4. 选择你要配置的仓库（在仓库选择器中选择）
5. 点击发送

Devin 会自动分析你的代码仓库，生成一个配置方案（Blueprint），然后展示给你看。你需要：

1. 检查 Devin 提出的配置方案
2. 点击 **Approve**（批准）按钮确认
3. 等待构建完成（通常 2-10 分钟）

构建完成后，Devin 的环境就配置好了。

### 方式二：手动配置（适合有经验的用户）

1. 在左侧边栏点击 **Settings**
2. 找到 **Environment** → **Blueprints**
3. 点击 **Add** 添加仓库
4. 选择你要配置的仓库
5. 在 YAML 编辑器中编写配置

一个典型的配置文件长这样：

```yaml
# 安装工具和运行时（只在构建时执行一次）
initialize: |
  curl -LsSf https://astral.sh/uv/install.sh | sh
  apt-get update && apt-get install -y nodejs npm

# 维护依赖（每次 session 启动时可能需要更新）
maintenance: |
  uv sync
  npm install

# 知识条目（告诉 Devin 如何操作你的项目）
knowledge:
  - name: lint
    contents: uv run ruff check .
  - name: test
    contents: uv run pytest
  - name: build
    contents: npm run build
  - name: dev
    contents: npm run dev
```

6. 点击 **Save** 保存
7. 等待构建完成

---

## 2.3 使用经典向导配置（Classic Configuration）

如果你更喜欢图形界面一步步操作：

### 第一步：进入 Devin's Machine

1. 在左侧边栏点击 **Settings**
2. 找到 **Devin's Machine**（Devin 的机器）
3. 点击 **Add repository**（添加仓库）

### 第二步：选择仓库

在弹出的仓库列表中，勾选你要添加的仓库，点击 **Start** 开始配置。

### 第三步：完成 8 个配置步骤

Devin 会引导你完成 8 个步骤：

#### 步骤 1：Git Pull
- 这是 Devin 每次启动 session 时执行的命令，用来拉取最新代码
- 通常保持默认的 `git pull` 即可
- 如果你的仓库有子模块（submodule），需要加上 `--recurse-submodules`

#### 步骤 2：配置 Secrets（密钥）
- 如果你的项目需要 API Key、数据库密码等，在这里配置
- 点击 **Add Secret** 按钮
- 输入 Secret 名称（如 `DATABASE_URL`）和值
- 也可以之后在 Settings > Secrets 中添加

#### 步骤 3：安装依赖
- Devin 会自动检测你的项目类型并建议命令
- Node.js 项目：通常是 `npm install`
- Python 项目：通常是 `pip install -r requirements.txt`
- 检查建议的命令是否正确，必要时修改

#### 步骤 4：维护依赖
- 这个命令在每次 session 启动时运行，确保依赖是最新的
- 通常和步骤 3 的命令相同

#### 步骤 5：配置 Lint
- 告诉 Devin 如何检查代码质量
- Node.js 项目：通常是 `npm run lint`
- Python 项目：通常是 `flake8` 或 `ruff check .`
- 如果没有 lint 命令，可以用 build 命令代替（检查编译错误）

#### 步骤 6：配置 Tests
- 告诉 Devin 如何运行测试
- Node.js 项目：通常是 `npm test`
- Python 项目：通常是 `pytest`
- Devin 在提交代码前会自动运行这些测试

#### 步骤 7：运行本地应用
- 告诉 Devin 如何启动你的应用
- Node.js 项目：通常是 `npm run dev`
- 这让 Devin 可以在浏览器中测试它的修改

#### 步骤 8：附加说明
- 任何额外的指令
- 比如："我们使用 Conventional Commits 规范"
- 或者："PR 标题格式为 [类型] 描述"

### 第四步：完成配置

- 点击 **Finish Setup** 保存配置
- Devin 会执行所有配置命令来构建快照
- 等待构建完成

---

## 2.4 添加 Secrets（密钥）

如果你的项目需要 API Key 或其他密钥：

1. 进入 **Settings** → **Secrets**
2. 点击 **Add Secret** 按钮
3. 填写：
   - **Name**：密钥名称，如 `OPENAI_API_KEY`
   - **Value**：密钥值
   - **Note**：说明（可选，帮助团队成员理解这个密钥的用途）
   - **Scope**：选择 Personal（仅自己可用）或 Organization（全组织可用）
4. 点击 **Save** 保存

注意：新添加的密钥只在**新创建的 session** 中可用，已有 session 不会自动获得。

---

## 2.5 验证环境是否配置成功

配置完成后，创建一个新的 session 来验证：

1. 点击 **New Session**
2. 选择 **Agent** 模式
3. 选择你刚才配置的仓库
4. 输入：

```
请运行这个项目的 lint 和 test 命令，确认环境配置正确。
```

5. 如果 lint 和 test 都能正常运行，说明环境配置成功

如果出错，检查：
- 依赖是否正确安装
- 环境变量是否正确配置
- 命令是否正确

---

## 2.6 关于快照（Snapshot）的重要概念

理解以下概念对使用 Devin 非常重要：

### 快照是只读的
- 每个 session 从快照启动一个**全新的副本**
- session 中的修改**不会**影响快照
- 下次 session 还是从同一个快照启动

### 快照会自动更新
- 当你修改配置时，会自动构建新的快照
- 大约每 24 小时也会自动重建一次（保持依赖最新）

### 每个组织只有一个活跃快照
- 同一个组织内的所有 session 都从同一个快照启动
- 如果配置出错，可以在 **Settings** → **Devin's Machine** → **Version History** 中回滚到之前的版本

---

## 2.7 小结

配置完成后，你的 Devin 已经：

1. 克隆了你的仓库
2. 安装了所有依赖
3. 知道如何运行 lint、test、build
4. 有了必要的密钥和环境变量

现在 Devin 可以真正开始帮你写代码了！下一步：学习如何给 Devin 布置任务。
